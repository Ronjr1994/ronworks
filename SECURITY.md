# Security Notes

- Never commit `AUTH_USERNAME`, `AUTH_PASSWORD`, API keys, or tokens.
- Store deployment secrets in Render Environment settings.
- Keep the GitHub repository private and grant Render access only to this repo.
- Rotate the password if it is shared accidentally.
- Only place browser-deliverable portfolio files inside `public/`.
